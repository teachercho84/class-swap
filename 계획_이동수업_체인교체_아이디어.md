# 이동수업 세트 개별교체 — 체인(연쇄) 교체 탐색 (아이디어 기록, 착수 보류)

## Context

지금 이동수업 세트(예: 교사 3명, 반 2개 A·B)를 다른 시간대로 옮기는 "개별 조합 교체"(`findMoveComboCandidates`)는 **대상 시간대의 A·B반 슬롯이 이미 완전히 비어 있는 경우만** 후보로 잡는다. 세트 대 세트 교체(`findMoveSwapCandidates`)는 제약상 어쩔 수 없이 그대로 두기로 했지만, 개별 조합 교체 쪽은 "대상 시간에 A·B반이 이미 비어 있어야만 매칭됨"이라는 조건 때문에 후보가 잘 안 나오는 경우가 많다는 게 사용자 문제의식.

논의를 통해 확인한 방향: 대상 시간 T에 A반(또는 B반) 슬롯이 비어있지 않고 다른 교사가 수업 중이더라도, **그 교사(점유 교사)를 평범한 1:1 교체로 다른 시간에 옮겨 T를 비울 수 있다면** 그것도 유효한 해법으로 취급하자는 것. 세트 교체(방식1)는 그대로 두고, 개별 조합 교체(방식2)만 "필요하면 사전 교체를 1~2건 끼워 넣는" 체인 방식으로 확장한다.

사용자가 직접 확인한 알고리즘 흐름:
1. 세트원 3명이 공통으로 비는 시간 T 후보들을 먼저 찾는다.
2. 각 T에 대해 A반·B반을 각각 그 시간에 담당하는 교사(점유 교사)가 누구인지 역조회한다.
3. 점유 교사가 이미 비어있으면 그대로(기존 방식2와 동일). 비어있지 않으면, 그 점유 교사 한 명을 T에서 비우는 **평범한 1:1 교체**를 탐색해 끼워 넣는다.
4. A·B 양쪽 다 점유되어 있으면 **각각 독립적으로** 1:1 교체를 탐색하고, 두 결과가 같은 교사/칸을 건드리지 않는지(충돌) 확인해서 조합한다.

## 재사용 가능한 기존 코드 (grounding, 새로 안 만들어도 됨)

- **교사가 특정 슬롯에 비어있는지**: `isEmpty(map, key, day, period)` / 이동세트 인지형 `isEmptyOrOwnGroup(...)` — `data.js:167-176`
- **반+요일+교시 → 담당 교사 역조회**: 전용 함수는 없지만 `STATE.classScheduleMap`(`buildClassScheduleMap`, `data.js:139-148`, 생성 `app.js:1235`)에 `getRecord(classScheduleMap, className, day, period)`로 바로 조회 가능. `.teacher` 필드가 점유 교사. 이미 `allMembersFreeAt`(`matching.js:40-47`)에서 동일 패턴으로 쓰이고 있음.
- **평범한 1:1 교체 탐색(=점유 교사를 비우는 데 그대로 쓸 함수)**: `findNormalSwapCandidates(ctx, teacherScheduleMap, teacherNames, weekSlots, absences)` — `matching.js:21-38`. `ctx = {teacher: 점유교사, className: 점유교사가 T에 가르치는 반, day, period}` 로 호출하면 "점유교사가 T를 비우고 다른 시간으로 옮길 수 있는 상대 교사" 후보가 그대로 나옴 — 새 탐색 로직을 짤 필요 없이 기존 함수를 서브루틴으로 재호출하는 구조.
- **반별 후보 (day,period) 맵 + 교집합**: `findClassSubstituteCandidates`(`matching.js:109-130`, 반별 후보) + `findMoveComboCandidates`의 교집합 로직(`matching.js:136-174`, `commonKeys` 부분 `145-147`)이 현재 "완전히 빈 슬롯만" 후보로 잡는 부분 — 이걸 확장 지점으로 삼는다.
- **"세트원 전원 공통 여유시간" 자체를 구하는 범용 헬퍼는 없음** — `findClassSubstituteCandidates` 내부의 `groupMembers.every(...)` (`matching.js:118-121`, 교사 측 여유만 체크)가 가장 가까운 참고 코드. 새로 짤 부분.

## 확장 설계 (착수 시 이 순서로)

1. **Step A (신규, 작음)**: 세트원 3명의 teacherScheduleMap 기준 공통 여유 (day,period) 집합을 구하는 헬퍼. `isEmpty` + 세트원 순회로 교집합.
2. **Step B (신규, 작음)**: Step A의 각 T에 대해 A반·B반 각각 `getRecord(classScheduleMap, className, day, period)`로 점유 교사 조회. 비어있으면 null.
3. **Step C (재사용)**: 점유 교사가 있으면 그 교사에 대해 `findNormalSwapCandidates`를 호출해 "T를 비우는 사전 교체" 후보 목록을 얻음. A·B 각각 독립 호출.
4. **Step D (신규, 작음)**: A후보 × B후보 조합에서 같은 교사·같은 (day,period) 칸을 이중으로 건드리는 조합을 걸러냄(충돌 체크).
5. **Step E (UI)**: 결과를 "직접 매칭(0단계) / 사전교체 1건 필요 / 사전교체 2건 필요"로 구분해 보여줌. 기존 1·2·3순위 대강 후보 접어서 표시하는 패턴(최근 커밋 `d4fefbd`)을 참고해 확장.

## 보류 중인 세부 이슈 (착수 시 다시 논의)

- 점유 교사 본인이 또 다른 이동수업 세트에 속해 있으면 어떻게 할지(재귀적으로 세트 제약이 또 걸림) — 이번 범위에서는 제외하고 "점유 교사가 이동세트 소속이면 체인 후보에서 제외" 정도로 단순화할지 결정 필요.
- 세트가 반 3개 이상으로 늘어나는 경우 조합 폭발 가능성 — 지금은 A·B 2개 기준으로만 설계, 필요시 재검토.

## 검증 방법 (착수 시)

- 실제 데이터에서 기존 방식2가 후보를 못 찾던 이동수업 세트 케이스를 하나 골라, 체인 로직 적용 후 후보가 나오는지 콘솔/UI로 확인.
- 기존 방식2(직접 매칭) 결과가 체인 로직 도입 후에도 그대로 나오는지(회귀 없는지) 확인.
