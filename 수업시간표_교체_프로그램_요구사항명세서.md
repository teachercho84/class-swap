# 수업 시간표 교체 프로그램 — 요구사항 명세서

## 1. 개요

교사가 자신의 수업을 다른 시간으로 옮기거나(맞교체), 부득이한 경우 다른 교사가 대신 수업을 맡을 수 있도록(대강) 후보를 자동으로 찾아주는 웹 도구.

- 대상 사용자: 고등학교 교사 (영산고등학교)
- 배포 형태: **GitHub Pages 정적 웹페이지** (서버 없음, 순수 HTML/JS 단일 파일)
- 데이터 처리: 브라우저 내에서만 파싱/계산 (외부 전송 없음)

## 2. 데이터 소스

### 2.1 파일 위치
엑셀 파일(`2026학년도_2학기_시간표(교과매핑추가).xlsx`)을 같은 GitHub 레포에 함께 올려두고, 페이지 로드 시 `fetch()`로 자동으로 읽어온다. **사용자가 직접 업로드하는 과정은 없음.** (SheetJS 라이브러리로 파싱)

학기 중 시간표가 바뀌면 레포에 파일만 다시 교체(push)하면 된다.

### 2.2 사용 시트
| 시트명 | 용도 |
|---|---|
| `설정` | 학교명/학년도/학기 — 웹페이지 타이틀에 사용 (2.6 참고) |
| `교사별 시간표` | 교사 기준 시간표 파싱 (메인 데이터 소스) |
| `학급별 시간표` | 반(학급) 기준 시간표 파싱 (반 충돌 검사용) |
| `교사_담당교과` | 교사-담당교과 매핑 (대강 후보 매칭용) |

`전체 교사 시간표`, `전체 학급 시간표` 두 시트는 사용하지 않음 (한눈에 보기용 매트릭스라 파싱 대상 아님. 이동수업 판별도 아래 2.5처럼 과목명만으로 가능해서 색상 파싱 자체가 불필요함).

### 2.3 `교사별 시간표` / `학급별 시간표` 구조 (중요 — 파싱 규칙)

**18행이 한 블록**, 교사(또는 반) 한 명당 하나의 블록이 반복되는 구조.

```
블록 시작행(예: 1행): 제목행 ("교사 시간표" 또는 "학반 시간표")
+1행 (2행)          : D열에 교사명 또는 반명 (예: "서헌", "1-1")
+2행 (3행)          : 요일 헤더 (월/화/수/목/금 — B~F열)
+3행 (4행)          : 1교시 — 과목명 (B~F열, 요일별)
+4행 (5행)          : 1교시 — 반 정보(교사별 시트) 또는 교사명(학급별 시트)
+5행 (6행)          : 2교시 — 과목명
+6행 (7행)          : 2교시 — 반/교사 정보
... (교시당 2행씩, 1~7교시 반복)
+17행 (18행)        : 빈 행 (블록 구분용)
```

- `교사별 시간표`: 23개 블록 (A1:AN51 범위 아님 — 실제 사용 데이터는 F열까지), 총 414행
- `학급별 시간표`: 9개 블록, 총 162행
- 교시는 1~7교시, 요일은 월~금 (B~F열)
- 과목 칸 값이 **"공강"** = 해당 시간 수업 없음
- 과목 칸 값이 **"창체"** = 창의적 체험활동 (담당 교과 개념 없음 → 교체/대강 대상에서 제외)

### 2.4 `교사_담당교과` 시트 구조

| 열 | 내용 |
|---|---|
| A. 교사명 | `교사별 시간표`의 교사명과 정확히 일치 |
| B. 담당교과 | 국어/수학/영어/사회/과학/체육/예술/진로/보건/제2외국어/기타 중 하나 |
| C. 비고 | 복수 교과 담당 시 추가 교과 (쉼표 구분) |
### 2.5 이동수업 판별 (과목명 기반, 색상 파싱 불필요)

**핵심 규칙: 과목명이 대문자로 끝나되, 그 글자가 `I` 또는 `V`가 아니면 이동수업.** 그 외(이니셜 없는 일반 과목 + 미술창작/음악연주/일본어회화 같은 특별실 과목 포함 전부)는 전부 **일반수업**으로 취급 — 별도 제외 없이 6.2의 일반 맞교체 로직을 그대로 적용한다.

> ⚠️ **로마자 숫자(과목 레벨 표기)와 헷갈리면 안 됨.** `미적I`, `영어II`, `화IIB`처럼 과목명에 로마자 숫자(`I`/`II`/`III`/`IV`)가 붙는 경우가 있는데, 이건 이동수업 태그가 아니라 과목 레벨이다. 실제 이 학교 과목 레벨은 `I`~`IV`까지만 쓰이므로, 로마자 숫자의 마지막 글자는 항상 `I`(I·II·III) 또는 `V`(IV) 둘 중 하나다. 따라서:
> - 끝 글자가 `I` 또는 `V` → 과목 레벨 표기, 이동수업 아님 (예: `미적I`, `영어II`)
> - 끝 글자가 그 외 대문자(`A`,`B`,`C`,`D`,`E`... 몇 개든 확장 가능) → 이동수업 태그 (예: `화학A`, `화IIB` — `화IIB`는 로마자 `II` 뒤에 태그 `B`가 붙은 형태)
>
> 정규식 예: 끝 글자를 뽑아서 `/^[A-Z]$/.test(char) && char !== 'I' && char !== 'V'` 이면 이동수업. (앞뒤 공백/개행은 트림 후 판정)

**세트(인스턴스) 묶는 방법**: 같은 `(요일, 교시)`에 이동수업 태그를 가진 교사들을 모으되, **학년이 다르면 다른 세트**다 — 같은 태그 글자라도 2학년 세트와 3학년 세트는 별개. 학년은 반 이름(`className`)의 앞자리 숫자로 판별 (예: `2-3` → 2학년).

```
moveGroupId = `${요일}_${교시}_${학년}_${끝글자}`   // 예: "월_2_2_A"
```

같은 `moveGroupId`를 가진 모든 (교사, 과목, 반) 레코드가 한 세트의 멤버다. 같은 태그 글자라도 요일/교시가 다르면 그 세트가 주중에 반복 등장하는 것뿐이니 인스턴스별로(=`moveGroupId`별로) 따로 다룬다.

예시 (2026학년도 2학기 실제 데이터, 참고용):
```
moveGroupId "월_2_2_A" → [세계사A-박현지-2-3반, 법과사회A-정우영-2-2반, 화학A-정예찬-2-1반]
  (같은 세트가 화_6_2_A, 목_6_2_A로도 반복 등장 — 주 3회)
```

### 2.6 `설정` 시트 구조 (페이지 타이틀용)

| A열 (항목) | B열 (값, 예시) |
|---|---|
| 학교명 | 영산고등학교 |
| 학년도 | 2026 |
| 학기 | 2학기 |

페이지 로드 시 이 세 값을 읽어 `{학년도}학년도 {학기} {학교명} 수업 시간표 교체`처럼 조합해서 `<title>`과 페이지 상단 헤더에 표시한다. 학기가 바뀌면 이 시트 값만 수정하면 코드 변경 없이 타이틀이 자동 반영됨.

## 3. 데이터 모델

파싱 후 아래 형태의 레코드 배열로 정규화한다.

```js
{
  teacher: "서헌",      // 교사명
  day: "화",            // 월/화/수/목/금
  period: 2,            // 1~7
  subject: "공국2",      // 과목명
  className: "1-1",     // 반
  isFree: false,        // subject === "공강" 인 경우 true
  isChangChe: false,    // subject === "창체" 인 경우 true
  moveGroupId: null     // subject 끝 글자가 대문자이고 I/V가 아니면 `${요일}_${교시}_${학년}_${끝글자}`, 아니면 null (2.5 참고)
}
```

`moveGroupId`는 `교사별 시간표` 파싱 시점에 과목명 끝 글자만 보고 바로 계산해서 채운다 (별도 시트/색상 조회 불필요).

같은 데이터를 반(class) 기준으로도 인덱싱해서, "특정 반이 특정 요일/교시에 비어있는지"를 O(1)로 조회할 수 있게 구성 (Map 또는 dict 추천: `teacherScheduleMap[교사][요일][교시]`, `classScheduleMap[반][요일][교시]`).

**이동수업 세트 데이터**는 별도로 인덱싱한다:
```js
// moveGroupId -> 그 세트에 속한 멤버 목록
{
  "월_2_2_A": [
    { teacher: "박현지", subject: "세계사A", className: "2-3" },
    { teacher: "정우영", subject: "법과사회A", className: "2-2" },
    { teacher: "정예찬", subject: "화학A", className: "2-1" }
  ]
}
```

## 4. 기술 스택

- 순수 HTML/CSS/JS 단일 파일 (React 불필요할 정도로 단순함 — 필요시 사용 가능)
- 엑셀 파싱: **SheetJS (xlsx.js)**
- 서버/백엔드 없음
- 배포: GitHub Pages (레포에 `index.html` + 엑셀 파일 함께 push)

## 5. UI 설계

페이지 상단에 `설정` 시트 값으로 조합한 타이틀 표시 (2.6 참고).

### 5.1 교사 선택
페이지 상단에 교사 선택 드롭다운 (23명 목록) → 선택 시 해당 교사의 시간표 그리드 표시.
*(로그인 기능 없음 — 본인 이름을 직접 선택하는 방식으로 가정. 다른 방식을 원하면 별도 지정 필요)*

### 5.2 시간표 그리드
- 월~금 × 1~7교시 그리드
- **수업이 있는 셀만 버튼**(클릭 가능), 과목명 + 반 표시
- **공강** 셀: 비활성화 (회색, 클릭 불가)
- **창체** 셀: 비활성화 (교체/대강 대상 아님)
- **이동수업(`moveGroupId` 있음)** 셀: 클릭 가능하되, 일반 수업과 구분되는 표시(예: 아이콘·테두리) + "이동수업 세트" 안내. 클릭 시 6.3 로직으로 세트 전체 후보 탐색
- 그 외 모든 수업(연노랑으로 표시됐던 미술/음악/일본어회화 포함)은 **일반 수업**으로 취급, 6.2 로직 그대로

### 5.3 후보 리스트 (셀 클릭 시)
클릭한 셀을 기준으로 아래 3단계 우선순위로 후보를 탐색해 표시한다.

```
[1순위: 맞교체 가능]
- 김이경 교사 — 목요일 3교시 (2-2반 문학)

[2순위: 동교과 대강 후보]  ← 1순위가 0건일 때만 탐색/표시
- (없음)

[3순위: 전체 대강 후보 — 교과 무관, 참고용]  ← 2순위도 0건일 때만 탐색/표시
- 박현지 교사 — 화요일 2교시 비어있음 (사회)
```
3순위 후보에는 "교과가 다를 수 있으니 참고만 하세요" 안내 문구를 붙인다.

이동수업 셀을 클릭했을 때 1순위 표시 예시:
```
[1순위: 세트 이동/교체 가능]
- 빈 시간대 이동: 수요일 6교시 (세트 전원 비어있음)
- 세트간 교체: "생윤A/물IA/생명IIA" 세트 ↔ 월요일 7교시
```

## 6. 핵심 매칭 로직

### 6.1 탐색 범위 및 공통 제외 대상
- **같은 주 전체** (월~금, 1~7교시) 전부 탐색. 요일 제한 없음.
- **창체**만 교체/대강 대상에서 완전 제외. (그 외에는 이동수업 포함 전부 대상)
- 클릭한 셀이 **이동수업(`moveGroupId` 있음)인지 아닌지**에 따라 이후 로직이 갈린다 → 이동수업이면 6.3으로, 아니면 6.2로.

### 6.2 일반 수업 — 1순위 맞교체 후보 (`moveGroupId`가 없는 경우)

**맞교체의 의미**: 반의 시간표 자체는 바뀌지 않는다. 그 시간에 "누가 들어가서 가르치는지"만 두 교사 사이에서 서로 바뀌는 것 — 즉 X가 내 원래 시간대에 들어와 내 반을 대신 맡고, 그 대가로 나는 X의 원래 시간대에 들어가 X의 반을 대신 맡는다. 반이 실제로 다른 시간으로 이동하는 게 아니므로, 반 단위 시간 충돌은 애초에 발생하지 않는다.

**단, 후보 X는 반드시 내 원래 반을 다른 시간에 이미 가르치고 있는 교사여야 한다** — 맞교체는 원래 그 반을 담당하는 교사끼리 시간만 바꾸는 것이지, 그 반과 무관한 교사가 갑자기 들어오는 게 아니다. (예: 1-2반은 서헌 외에도 윤은정·박현지·김재미 등 여러 교사가 다른 요일/교시에 들어간다 — 이 중에서만 후보를 찾는다.)

클릭한 슬롯을 `(나, 원래요일, 원래교시, 원래반)`이라 할 때, 다른 교사 `X`의 `(대상요일, 대상교시)`가 아래 **3가지 조건을 모두** 만족해야 후보로 채택:

1. 교사 `X`가 `(대상요일, 대상교시)`에 실제 수업 중이고, 그 수업의 반이 **원래반과 정확히 같음** — X가 원래반을 다른 시간에 담당하고 있어야 함
2. 나는 `(대상요일, 대상교시)`에 비어있음 (공강이거나 수업 없음) — X의 시간에 내가 들어갈 수 있어야 함
3. 교사 `X`는 `(원래요일, 원래교시)`에 비어있음 — 내 시간에 X가 들어올 수 있어야 함

3개 조건을 모두 통과하는 모든 `(X, 대상요일, 대상교시)` 조합을 후보로 나열. 대상 `(X, 대상요일, 대상교시)`가 **이동수업 세트 소속이면 후보에서 제외** (세트는 개별 1:1 교체 대상이 아님 — 6.3에서 별도 처리). 대상 슬롯이 **공강·창체**인 경우도 제외 (실제 수업 내용이 없어 교체 대상이 될 수 없음).

결과가 0건이면 6.4(동교과 대강)로 진행.

> 참고: 6.3(이동수업 세트)은 이 모델과 다르다 — 이동수업은 여러 반의 학생이 함께 모이는 하나의 실제 수업(세트)이 통째로 다른 시간으로 옮겨가거나 다른 세트와 맞바뀌는 것이라, 세트에 속한 교사·반 전원이 실제로 그 시간에 이동 가능해야 한다(6.3 조건 그대로 유지, 반 충돌 검사 포함).

### 6.3 이동수업 — 1순위 맞교체 후보 (`moveGroupId`가 있는 경우)

클릭한 셀이 속한 세트 `A = moveGroupData[moveGroupId]` (교사·과목·반 목록)를 통째로 다른 시간대로 옮긴다. 두 가지 방식으로 후보를 찾는다.

**방식 1) 빈 시간대로 이동**
세트 A의 원래 시간(`origDay, origPeriod`)을 제외한 모든 `(대상요일, 대상교시)`에 대해:
- 세트 A에 속한 **모든 교사**가 그 시간에 비어있고
- 세트 A에 속한 **모든 반**이 그 시간에 비어있으면

→ 후보로 채택 ("빈 시간대 이동" 유형으로 표시)

**방식 2) 다른 이동수업 세트와 맞교체**
다른 이동수업 세트 `B` (시간 `targetDay, targetPeriod`)가 존재할 때, 아래를 모두 만족하면 후보로 채택:
1. 세트 A의 모든 교사·반이 `(targetDay, targetPeriod)`에 비어있음 (단, 세트 B 멤버 본인들의 현재 수업은 "이동해서 비워질 것"이므로 충돌로 안 침 — 세트 B 자신은 예외 처리)
2. 세트 B의 모든 교사·반이 `(origDay, origPeriod)`에 비어있음 (마찬가지로 세트 A 자신은 예외)

→ 후보로 채택 시 두 세트가 서로의 시간대로 맞바뀜 ("세트간 교체" 유형으로 표시)

두 방식의 결과를 합쳐서 1순위 후보 리스트로 보여준다. 결과가 0건이면 6.4로 진행.

> 이동수업은 대강 로직(6.4·6.5)에는 영향 없음 — 대강은 시간을 바꾸는 게 아니라 그 시간에 다른 교사가 대신 들어가는 것이라 세트 전체를 건드릴 필요가 없음.

### 6.4 2순위 — 동교과 대강 후보 (1순위 결과 0건일 때만)

`(원래요일, 원래교시)`에 비어있는 교사 중, `교사_담당교과` 기준 **나와 같은 담당교과**인 교사를 후보로 나열. (반 충돌 검사 불필요 — 시간 이동이 아니라 그 시간에 대신 들어가는 것이므로)

> **예외: 원래 과목이 정확히 "진로"인 경우** — 담당교과 관계없이 **아무 교사나** 대강 가능한 특별 과목이다. 이 경우 동교과 필터를 건너뛰고, `(원래요일, 원래교시)`에 비어있는 교사 전원을 바로 2순위 후보로 나열한다 (3순위의 "참고용" 문구는 붙이지 않음 — 정식으로 아무나 가능한 것이므로). **"진로직업"은 이 예외에 해당하지 않음** — 일반 동교과 로직 그대로 적용.

### 6.5 3순위 — 전체 대강 후보 (2순위 결과도 0건일 때만)

`(원래요일, 원래교시)`에 비어있는 교사 전체(교과 무관)를 후보로 나열. 화면에 "참고용 — 교과가 다를 수 있음" 문구 표시.

## 7. 제외 범위 (이번 버전에서 구현하지 않음)

- 수정된 시간표를 엑셀 파일로 저장/내보내기
- 교체 안내문/공지 문서 자동 생성 (차후 기능)
- 실제 시간표 데이터의 영구 저장/반영 (조회·시뮬레이션 용도)
- 창체 시간 교체/대강 지원
- 로그인/인증 기능

## 8. 참고 파일

- `2026학년도_2학기_시간표(교과매핑추가).xlsx` — 원본 시간표 + `설정`/`교사_담당교과` 탭 추가된 최종 데이터 파일

## 9. 운영 방법 (매 학기 갱신 절차)

시간표 데이터는 전부 엑셀에서 동적으로 파싱하므로(교사 목록·시간표·이동수업 판별 모두 하드코딩 없음), 학기가 바뀌어도 아래 절차만 지키면 코드 수정 없이 새 학기 데이터가 반영된다.

1. **엑셀 파일명을 고정한다** (예: `data.xlsx`). `index.html`은 이 고정된 파일명을 `fetch()`로 읽도록 작성 — 학기마다 파일명이 바뀌면 코드도 같이 고쳐야 하므로, 반드시 같은 이름으로 덮어쓰기.
2. 새 학기 시간표가 나오면:
   - `설정` 탭의 학년도/학기 값 수정
   - `교사_담당교과` 탭 확인 — 교사 전입/전출, 담당교과 변경분만 수정 (새 교사는 행 추가, 퇴직/전출 교사는 행 삭제)
   - 완성된 파일을 같은 파일명으로 레포에 덮어쓰기(push)
3. 그 외(교사 목록, 시간표 그리드, 이동수업 세트, 맞교체/대강 로직)는 전부 자동으로 새 데이터 기준으로 재계산됨 — 코드 수정 불필요.

> 단, `교사별 시간표`/`학급별 시간표`의 **18행 블록 구조**(2.3)와 시트명이 이후 학기에도 동일한 형식으로 나온다는 전제. 학교 시간표 프로그램의 엑셀 내보내기 양식 자체가 바뀌면 파싱 로직도 같이 손봐야 한다.
